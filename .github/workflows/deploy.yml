steps:
  - name: Checkout
    uses: actions/checkout@v4 # Esta ya es la v4, está bien.

  - name: Setup Node
    uses: actions/setup-node@v4
    with:
      node-version: '24' # Cambia esto de 20 a 24

  - name: Install and Build
    run: |
      npm install
      npm run build
